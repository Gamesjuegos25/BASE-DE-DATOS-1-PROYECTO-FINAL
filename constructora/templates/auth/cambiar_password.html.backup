{% extends "base.html" %}

{% block title %}Cambiar Contraseña - ERP Constructora{% endblock %}

{% block content %}
<div class="page-header">
    <nav class="breadcrumb">
        <a href="{{ url_for('dashboard') }}">Dashboard</a>
        <span class="breadcrumb-separator">/</span>
        <span>Cambiar Contraseña</span>
    </nav>
    <h1 class="page-title">🔑 Cambiar Contraseña</h1>
    <p class="page-subtitle">Actualiza tu contraseña por seguridad</p>
</div>

<div class="form-container" style="max-width: 600px;">
    <form method="POST" id="formCambiarPassword" class="modern-form" novalidate>
        <div class="form-section">
            <h3 class="section-title">
                <i class="fas fa-shield-alt"></i>
                Cambio de Contraseña
            </h3>
            
            <div class="security-notice">
                <div class="notice-header">
                    <i class="fas fa-info-circle"></i>
                    <strong>Política de Contraseñas</strong>
                </div>
                <ul class="security-requirements">
                    <li><i class="fas fa-check"></i> Mínimo 8 caracteres</li>
                    <li><i class="fas fa-check"></i> Al menos una letra mayúscula</li>
                    <li><i class="fas fa-check"></i> Al menos una letra minúscula</li>
                    <li><i class="fas fa-check"></i> Al menos un número</li>
                    <li><i class="fas fa-check"></i> Al menos un carácter especial (!@#$%^&*)</li>
                </ul>
            </div>
            
            <div class="form-group">
                <label for="password_actual" class="form-label required">Contraseña Actual</label>
                <div class="password-input-container">
                    <input type="password" 
                           id="password_actual" 
                           name="password_actual" 
                           class="form-control" 
                           placeholder="Ingresa tu contraseña actual"
                           required 
                           autocomplete="current-password">
                    <button type="button" class="password-toggle" onclick="togglePassword('password_actual')">
                        <i class="fas fa-eye" id="icon_password_actual"></i>
                    </button>
                </div>
                <div class="form-help">Confirma tu identidad con tu contraseña actual</div>
                <div class="form-error" id="error-password_actual"></div>
            </div>
            
            <div class="form-group">
                <label for="password_nuevo" class="form-label required">Nueva Contraseña</label>
                <div class="password-input-container">
                    <input type="password" 
                           id="password_nuevo" 
                           name="password_nuevo" 
                           class="form-control" 
                           placeholder="Ingresa tu nueva contraseña"
                           required 
                           autocomplete="new-password"
                           minlength="8">
                    <button type="button" class="password-toggle" onclick="togglePassword('password_nuevo')">
                        <i class="fas fa-eye" id="icon_password_nuevo"></i>
                    </button>
                </div>
                <div class="password-strength" id="passwordStrength" style="display: none;">
                    <div class="strength-bar">
                        <div class="strength-fill" id="strengthFill"></div>
                    </div>
                    <div class="strength-text" id="strengthText">Muy débil</div>
                </div>
                <div class="form-error" id="error-password_nuevo"></div>
            </div>
            
            <div class="form-group">
                <label for="password_confirmacion" class="form-label required">Confirmar Nueva Contraseña</label>
                <div class="password-input-container">
                    <input type="password" 
                           id="password_confirmacion" 
                           name="password_confirmacion" 
                           class="form-control" 
                           placeholder="Confirma tu nueva contraseña"
                           required 
                           autocomplete="new-password">
                    <button type="button" class="password-toggle" onclick="togglePassword('password_confirmacion')">
                        <i class="fas fa-eye" id="icon_password_confirmacion"></i>
                    </button>
                </div>
                <div class="match-indicator" id="matchIndicator" style="display: none;">
                    <i class="fas fa-times-circle text-danger" id="matchIcon"></i>
                    <span id="matchText">Las contraseñas no coinciden</span>
                </div>
                <div class="form-error" id="error-password_confirmacion"></div>
            </div>
        </div>
        
        <!-- Recomendaciones de Seguridad -->
        <div class="form-section bg-gray-50">
            <h3 class="section-title">
                <i class="fas fa-lightbulb"></i>
                Recomendaciones de Seguridad
            </h3>
            
            <div class="security-tips">
                <div class="tip-item">
                    <i class="fas fa-check-circle text-success"></i>
                    <span>Usa una combinación única de letras, números y símbolos</span>
                </div>
                <div class="tip-item">
                    <i class="fas fa-check-circle text-success"></i>
                    <span>Evita usar información personal como fechas de nacimiento</span>
                </div>
                <div class="tip-item">
                    <i class="fas fa-check-circle text-success"></i>
                    <span>No reutilices contraseñas de otras cuentas</span>
                </div>
                <div class="tip-item">
                    <i class="fas fa-check-circle text-success"></i>
                    <span>Cambia tu contraseña regularmente (cada 90 días)</span>
                </div>
            </div>
            
            <div class="last-change-info" style="margin-top: var(--spacing-4);">
                <p><strong>Información de Cuenta:</strong></p>
                <div class="account-info">
                    <div><i class="fas fa-user"></i> Usuario: {{ session.usuario_nombre }}</div>
                    <div><i class="fas fa-envelope"></i> Email: {{ session.usuario_email }}</div>
                    <div><i class="fas fa-shield-alt"></i> Rol: {{ session.usuario_rol }}</div>
                </div>
            </div>
        </div>
        
        <div class="form-actions">
            <a href="{{ url_for('dashboard') }}" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i> Cancelar
            </a>
            <button type="submit" class="btn btn-primary" id="btnCambiar">
                <i class="fas fa-key"></i> Cambiar Contraseña
            </button>
        </div>
    </form>
</div>

<script>
// Variables del formulario
const form = document.getElementById('formCambiarPassword');
const btnCambiar = document.getElementById('btnCambiar');

// Elementos de contraseña
const passwordActual = document.getElementById('password_actual');
const passwordNuevo = document.getElementById('password_nuevo');
const passwordConfirmacion = document.getElementById('password_confirmacion');

// Indicadores
const passwordStrength = document.getElementById('passwordStrength');
const strengthFill = document.getElementById('strengthFill');
const strengthText = document.getElementById('strengthText');
const matchIndicator = document.getElementById('matchIndicator');
const matchIcon = document.getElementById('matchIcon');
const matchText = document.getElementById('matchText');

// Validación en tiempo real
passwordNuevo.addEventListener('input', function() {
    validarPasswordNuevo();
    verificarCoincidencia();
});

passwordConfirmacion.addEventListener('input', function() {
    verificarCoincidencia();
});

passwordActual.addEventListener('input', function() {
    validarCampo(this);
});

// Validar fortaleza de contraseña
function validarPasswordNuevo() {
    const password = passwordNuevo.value;
    passwordStrength.style.display = password.length > 0 ? 'block' : 'none';
    
    if (password.length === 0) return;
    
    let score = 0;
    let feedback = [];
    
    // Longitud
    if (password.length >= 8) score += 20;
    else feedback.push('Mínimo 8 caracteres');
    
    // Mayúsculas
    if (/[A-Z]/.test(password)) score += 20;
    else feedback.push('Al menos una mayúscula');
    
    // Minúsculas
    if (/[a-z]/.test(password)) score += 20;
    else feedback.push('Al menos una minúscula');
    
    // Números
    if (/[0-9]/.test(password)) score += 20;
    else feedback.push('Al menos un número');
    
    // Caracteres especiales
    if (/[!@#$%^&*(),.?":{}|<>]/.test(password)) score += 20;
    else feedback.push('Al menos un carácter especial');
    
    // Actualizar indicador visual
    let strength = '';
    let color = '';
    
    if (score < 40) {
        strength = 'Muy débil';
        color = '#dc3545';
    } else if (score < 60) {
        strength = 'Débil';
        color = '#fd7e14';
    } else if (score < 80) {
        strength = 'Regular';
        color = '#ffc107';
    } else if (score < 100) {
        strength = 'Buena';
        color = '#198754';
    } else {
        strength = 'Excelente';
        color = '#20c997';
    }
    
    strengthFill.style.width = score + '%';
    strengthFill.style.backgroundColor = color;
    strengthText.textContent = strength;
    strengthText.style.color = color;
    
    // Validar campo
    validarCampo(passwordNuevo, feedback.length === 0, feedback.join(', '));
}

// Verificar coincidencia de contraseñas
function verificarCoincidencia() {
    const nuevo = passwordNuevo.value;
    const confirmacion = passwordConfirmacion.value;
    
    if (confirmacion.length === 0) {
        matchIndicator.style.display = 'none';
        return;
    }
    
    matchIndicator.style.display = 'flex';
    
    if (nuevo === confirmacion) {
        matchIcon.className = 'fas fa-check-circle text-success';
        matchText.textContent = 'Las contraseñas coinciden';
        matchText.className = 'text-success';
        passwordConfirmacion.classList.remove('error');
    } else {
        matchIcon.className = 'fas fa-times-circle text-danger';
        matchText.textContent = 'Las contraseñas no coinciden';
        matchText.className = 'text-danger';
        passwordConfirmacion.classList.add('error');
    }
}

// Validar campo individual
function validarCampo(campo, esValido = null, mensaje = '') {
    const valor = campo.value.trim();
    const nombre = campo.name;
    const errorElement = document.getElementById(`error-${nombre}`);
    
    // Limpiar errores previos si no se especifica validación
    if (esValido === null) {
        campo.classList.remove('error');
        if (errorElement) errorElement.textContent = '';
        
        // Validaciones específicas
        switch (nombre) {
            case 'password_actual':
                if (!valor) {
                    campo.classList.add('error');
                    if (errorElement) errorElement.textContent = 'La contraseña actual es obligatoria';
                    return false;
                }
                break;
        }
        return true;
    }
    
    // Usar validación personalizada
    if (!esValido) {
        campo.classList.add('error');
        if (errorElement) errorElement.textContent = mensaje;
    } else {
        campo.classList.remove('error');
        if (errorElement) errorElement.textContent = '';
    }
    
    return esValido;
}

// Mostrar/ocultar contraseña
function togglePassword(fieldId) {
    const field = document.getElementById(fieldId);
    const icon = document.getElementById(`icon_${fieldId}`);
    
    if (field.type === 'password') {
        field.type = 'text';
        icon.classList.replace('fa-eye', 'fa-eye-slash');
    } else {
        field.type = 'password';
        icon.classList.replace('fa-eye-slash', 'fa-eye');
    }
}

// Validar formulario completo
function validarFormulario() {
    const actualValido = validarCampo(passwordActual);
    const nuevoValido = passwordNuevo.value.length >= 8 && 
                       /[A-Z]/.test(passwordNuevo.value) &&
                       /[a-z]/.test(passwordNuevo.value) &&
                       /[0-9]/.test(passwordNuevo.value) &&
                       /[!@#$%^&*(),.?":{}|<>]/.test(passwordNuevo.value);
    const coincidenciaValida = passwordNuevo.value === passwordConfirmacion.value;
    
    return actualValido && nuevoValido && coincidenciaValida;
}

// Envío del formulario
form.addEventListener('submit', function(e) {
    e.preventDefault();
    
    if (!validarFormulario()) {
        alert('Por favor, completa todos los campos correctamente.');
        return;
    }
    
    // Mostrar loading
    btnCambiar.disabled = true;
    btnCambiar.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Cambiando...';
    
    // Enviar formulario
    setTimeout(() => {
        form.submit();
    }, 1000);
});

// Focus automático
document.addEventListener('DOMContentLoaded', function() {
    passwordActual.focus();
});
</script>

<style>
.security-notice {
    background: linear-gradient(135deg, #e3f2fd 0%, #f3e5f5 100%);
    border: 1px solid var(--color-info);
    border-radius: var(--border-radius);
    padding: var(--spacing-4);
    margin-bottom: var(--spacing-4);
}

.notice-header {
    display: flex;
    align-items: center;
    gap: var(--spacing-2);
    margin-bottom: var(--spacing-3);
    color: var(--color-info);
    font-weight: 600;
}

.security-requirements {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--spacing-2);
}

.security-requirements li {
    display: flex;
    align-items: center;
    gap: var(--spacing-2);
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
}

.security-requirements i {
    color: var(--color-success);
    font-size: var(--font-size-xs);
}

.password-input-container {
    position: relative;
}

.password-toggle {
    position: absolute;
    right: var(--spacing-3);
    top: 50%;
    transform: translateY(-50%);
    background: none;
    border: none;
    color: var(--color-text-secondary);
    cursor: pointer;
    padding: var(--spacing-1);
}

.password-toggle:hover {
    color: var(--color-primary);
}

.password-strength {
    margin-top: var(--spacing-2);
}

.strength-bar {
    height: 4px;
    background: var(--color-gray-200);
    border-radius: 2px;
    overflow: hidden;
    margin-bottom: var(--spacing-1);
}

.strength-fill {
    height: 100%;
    transition: all 0.3s ease;
    border-radius: 2px;
}

.strength-text {
    font-size: var(--font-size-sm);
    font-weight: 500;
}

.match-indicator {
    display: flex;
    align-items: center;
    gap: var(--spacing-2);
    margin-top: var(--spacing-2);
    font-size: var(--font-size-sm);
}

.security-tips {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-2);
}

.tip-item {
    display: flex;
    align-items: flex-start;
    gap: var(--spacing-2);
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
}

.tip-item i {
    margin-top: 2px;
    flex-shrink: 0;
}

.account-info {
    display: flex;
    flex-direction: column;
    gap: var(--spacing-2);
    margin-top: var(--spacing-2);
    font-size: var(--font-size-sm);
    color: var(--color-text-secondary);
}

.account-info div {
    display: flex;
    align-items: center;
    gap: var(--spacing-2);
}

.account-info i {
    width: 16px;
    color: var(--color-primary);
}

@media (max-width: 768px) {
    .security-requirements {
        grid-template-columns: 1fr;
    }
}
</style>
{% endblock %}